<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name=viewport content="width=device-width,initial-scale=1">
  <title>{{title}}</title>
  <link href="{{css}}" rel="stylesheet">
</head>
<body>
    {{> navbar}}
    <div class="container-fluid">
        <div class="row flex-center">
            <div id="vue-app" class="col-8 offset-2">
                <alias-list v-bind:endpoint="'{{endpoint}}'"  />
            </div>
        </div>
    </div>
    <script src="{{js}}"></script>
</body>
</html>
