<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name=viewport content="width=device-width,initial-scale=1">
  <title>{{title}}</title>
  <link href="{{css}}" rel="stylesheet">
</head>
<body>
    {{> navbar}}

    <div class="container-fluid">
        <div class="row flex-center">
            <div class="col">
              <h2>{{pages.search.title}}</h2>
              <p>
                {{pages.search.intro}}
              </p>
            </div>
        </div>
        <div class="row">
            <div id="vue-app"  class="col col-8 offset-2">
                <Search v-bind:endpoint="'{{endpoint}}/search'" />
            </div>
        </div>
    </div>
    <script src="{{js}}"></script>
</body>
</html>
